# Release Checklist

- Tests pass with `./.venv/bin/python -m pytest -q`
- `make test` passes
- GitHub Actions is green
- `README.md` is updated
- `CHANGELOG.md` is updated
- Generated data files are not committed
- Release tag is created
- GitHub release can be drafted
